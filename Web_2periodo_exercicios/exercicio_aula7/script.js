//1.Crie um script JS usando o DOM que leia o nome de uma pessoa e mostre uma  mensagem de Boas Vindas de acordo com o nome digitado.
 
/*function boasVindas () {
    var nome = document.querySelector('#nome').value
    document.write(`Boas vindas ${nome}`);
}*/
 

//2.Ler dois valores a partir do input, calcular e imprimir na tela usando o DOM:
// a) A soma destes valores;
// b)O produto deles;
// c)O quociente entre eles;
// d)A diferença entre eles.

/*function somar () {
    let n1 = document.querySelector('#n1').value
    let n2 = document.querySelector('#n2').value
    let soma = Number(n1) + Number(n2);
    let mult = Number(n1) * Number(n2);
    let divide = Number(n1) / Number(n2);
    let subtracao = Number(n1) - Number(n2);
    
    document.write(`${n1} + ${n2} = ${soma}`);
    document.write("<br>");
    document.write(`${n1} * ${n2} = ${mult}`);
    document.write("<br>");
    document.write(`${n1} / ${n2} = ${divide}`);
    document.write("<br>");
    document.write(`${n1} - ${n2} = ${subtracao}`);
}*/

//3.Elabore um programa usando o DOM que calcule a média aritmética de duas notas e exiba o resultado na tela.

   /* let resp = document.querySelector('#resp');

    function calcularMedia(){
        let num1 = Number(document.querySelector('#num1').value);
        let num2 = Number(document.querySelector('#num2').value);

        let media = (num1 + num2) / 2;

        resp.innerText = 'A média é ' + media;
    }*/